const welcome = (number, groupname) => {
    return `𝗛𝗼𝗹𝗮 @${number}. 𝐒𝐞𝐚 𝗯𝗶𝗲𝗻𝘃𝗲𝗻𝗶𝗱𝗼 𝐚𝗹 𝐠𝐫𝐮𝐩𝐨 ${groupname}`
}
exports.welcome = welcome

const bye = (number) => {
    return `Adios @${number}. ya era hora deque te fueras idiota👋`
}
exports.bye = bye